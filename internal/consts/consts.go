package consts

const (
	// Constant keys

	// API_URL is a key used to lookup the api url
	API_URL = "API_URL"
	// API_PORT is a key used to lookup the api port
	API_PORT = "API_PORT"
	// CERT_DIR is a key used to lookup the certificate directory
	CERT_DIR = "CERT_DIR"
	// CLIENT_CERT is a key used to lookup the data-collector client certificate
	CLIENT_CERT = "CLIENT_CERT"
	// API_CERT is a key used to lookup the api certificate file
	API_CERT = "API_CERT"
	// API_KEY is a key used to lookup the api certificate key
	API_KEY = "API_KEY"
	// DB_URI is a key used to lookup the database URI
	DB_URI = "DB_URI"
	// DB_NAME is a key used to lookup the database name
	DB_NAME = "DB_NAME"
	// DB_USER is a key used to lookup the database user
	DB_USER = "DB_USER"
	// DB_PASS is a key used to lookup the database pass
	DB_PASS = "DB_PASS"
	// LOG_FILE is a key used to lookup the location of the log file
	LOG_FILE = "LOG_FILE"

	// Constant filenames

	// AGENT_STORE_FILENAME is the file location of agent filestore
	AGENT_STORE_FILENAME = "config.json"

	// Constant collection names

	// COLLECTION_HEALTH is the collection name used for health collection
	COLLECTION_HEALTH = "health"
)
